# Dataset

This project was trained and evaluated using the CASIA 2.0 Image Tampering Dataset.

The dataset is not included in this repository due to its size and licensing restrictions.

Dataset structure used during training:

dataset/
├── train/
├── validation/
└── test/
